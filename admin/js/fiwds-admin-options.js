jQuery(document).ready(function($) {

/*
	// Hide parts of form on load
	$( '.fiwds_size_options' ).hide();
	$( '.fiwds_size_details' ).hide();

	if ($( 'input[name="chk[]"]:checked' ).length == 0) {
		alert('checked');
	}
	
	// When FIWDS primary checkbox change, display or hide secondary option checkbox
	$('.fiwds_checkbox_img_required').change(function() {
		if ($(this).is(':checked')) {
			$(this).closest('.fiwds_checkbox_required_parent').next('.fiwds_size_options').slideDown();
		} else {
			$(this).closest('.fiwds_checkbox_required_parent').next('.fiwds_size_options').slideUp();
		}
	});

	// When FIWDS secondary option change, display or hide size panel
	$('.fiwds_checkbox_size_required').change(function() {
		if ($(this).is(':checked')) {
			$(this).closest('.fiwds_size_options').children('.fiwds_size_details').slideDown();
		} else {
			$(this).closest('.fiwds_size_options').children('.fiwds_size_details').slideUp();
		}
	});
*/
});